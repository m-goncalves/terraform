variable "username" {
    type = string
    description = "username for rds"
    default = "just to remember the variable needed - use ssm"

}

variable "password" {
    type = string
    description = "password for rds"
    default = "just to remember the variables needed - use ssm"
  
}
